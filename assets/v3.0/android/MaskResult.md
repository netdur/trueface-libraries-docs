# MaskResult

Result of a mask detection operation.

## Fields

- `MaskLabel label` - The detection result. See [MaskLabel](/v3.0/android/MaskLabel).
- `double score` - The confidence score of the detection.
