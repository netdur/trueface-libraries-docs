# Precision

Precision to use for GPU inference. Annotated `@UnsupportedOnMobile`.

## Values

- `FP32` - 32-bit floating point. Highest accuracy but slower inference.
- `FP16` - 16-bit floating point. Faster inference but lower accuracy.
