# FaceImageBlurDetectionResult

## `public class FaceImageBlurDetectionResult`

This class represents the result of a face image blur detection operation, containing the quality of the image and a blur score.

### Fields

#### `public FaceImageQuality quality`

The quality of the face image.

#### `public float score`

The blur score of the face image.

### Constructors

#### `public FaceImageBlurDetectionResult()`

Default constructor.

#### `public FaceImageBlurDetectionResult(FaceImageQuality quality, float score)`

Constructor to initialize the `FaceImageBlurDetectionResult` with specific values.

- **Parameters:**
  - `quality` - The quality of the face image.
  - `score` - The blur score of the face image.